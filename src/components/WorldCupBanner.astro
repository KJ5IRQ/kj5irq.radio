<aside class="wc-banner" role="note" aria-label="World Cup 2026">
  <p class="meta wc-line">
    <span aria-hidden="true">⚽</span> Scotland · World Cup 2026 · <span lang="gd">Alba gu bràth</span>
  </p>
</aside>

<style>
  .wc-banner {
    border-bottom: 1px solid var(--rule);
    background: var(--surface);
  }

  .wc-line {
    max-width: var(--page-max);
    margin-inline: auto;
    padding: var(--space-2) var(--space-5);
    text-align: center;
    color: var(--accent);
  }
</style>
