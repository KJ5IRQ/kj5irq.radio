{
  "name": "kj5irq.radio",
  "version": "1.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "astro dev --host 0.0.0.0 --port 8080",
    "build": "node scripts/check-source.mjs && astro build && node scripts/privacy-grep.mjs && node scripts/check-links.mjs",
    "check": "astro check",
    "preview": "astro preview --host 0.0.0.0 --port 8080",
    "sync": "node scripts/vault-sync.mjs"
  },
  "dependencies": {
    "@astrojs/sitemap": "^3.4.0",
    "@fontsource-variable/source-serif-4": "^5.2.0",
    "@fontsource/ibm-plex-mono": "^5.2.0",
    "astro": "^5.8.0"
  }
}
